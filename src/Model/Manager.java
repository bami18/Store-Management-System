package Model;

import enums.Sex;

public class Manager extends Staff{
    public Manager(String name, int age, String phoneNumber, Sex sex, int idNumber) {
        super(name, age, phoneNumber, sex, idNumber);
    }
}
